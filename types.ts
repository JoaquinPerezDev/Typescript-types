const today = new Date();
today.getMonth();

const person = {
  age: 20
};

class Color {

}

const red = new Color();

//TS Types: Primitive or Object
//Primitive Types: number, boolean, void, undefined, string, symbol, null
//Object Types: functions, arrays, classes, objects